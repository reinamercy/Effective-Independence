# Pinned dependencies for the Effective Independence project.
# Versions verified against PyPI on 2026-08-11.

pyyaml==6.0.2
requests==2.32.3
tenacity==9.1.4
datasets==3.2.0
tqdm==4.67.1
numpy==2.1.3
pandas==2.2.3
scipy==1.14.1
matplotlib==3.9.3
seaborn==0.13.2
python-dotenv==1.0.1
jsonlines==4.0.0
truststore==0.10.1

# API SDKs — pinned to latest 1.x/0.x lines (not the newer 2.x majors) since
# api_client.py targets the well-established chat.completions / messages.create
# surface; re-verify compatibility before bumping to a new major.
anthropic==0.121.0
openai==1.109.1
google-genai==1.75.0

# Grading
math_verify==0.9.0
